[package]
name = "b2c-cod-hider"
version = "1.0.0"
edition = "2021"

[dependencies]
shopify_function = "2.1.0"
serde = { version = "1", features = ["derive"] }

[lib]
name = "b2c_cod_hider"
crate-type = ["cdylib"]

[profile.release]
lto = true
opt-level = 'z'
strip = true
